## Tech Stack (nextjs)

- Next.js 15 (App Router) + TypeScript strict
- Tailwind CSS v4 (CSS-first) + shadcn/ui
- Vitest + React Testing Library for tests
- Playwright for E2E (optional)
- pnpm for package management

## Architecture

```
src/
  app/
    [locale]/    # i18n routing (ro | ru) — ALL pages live here
    layout.tsx   # Root layout
    globals.css  # Tailwind v4 @theme + CSS custom properties
  components/    # React components (shadcn/ui based), organized by domain
  lib/           # Shared utilities, types, constants, i18n
  hooks/         # Custom React hooks
```

## Additional Conventions

### Testing
- Single Vitest runner for all tests
- Globals enabled (no need to import describe/it/expect)
- `pnpm vitest run <file>` for single file
- Tests in `__tests__/` subdirectories or co-located as `*.test.ts(x)`
